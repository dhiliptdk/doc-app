import { SortRangesPipe } from './sort-ranges.pipe';

describe('SortRangesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRangesPipe();
    expect(pipe).toBeTruthy();
  });
});
